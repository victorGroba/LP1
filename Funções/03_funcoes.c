#include <stdio.h>
#include <string.h>
#include <locale.h>

int main() {
  /*Parâmetros da função:
  permitemm ao programador passar uma informação de 
  um trecho de código para dentro da função
  */
 float raiz(float r);
 int soma(int, x);
 int soma(int x, y)// ERRADO
 float exponencial(float a, int b);
 

    return 0;
}